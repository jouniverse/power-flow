{
  "framework": null,
  "buildCommand": "mkdir -p public && cp *.html public/ && cp -r css js imgs public/",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/proxy?_proxyPath=:path*" }
  ]
}
